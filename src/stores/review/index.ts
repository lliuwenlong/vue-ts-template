import { defineStore } from 'pinia';

const useReviewStore = defineStore('review', {
    state: () => {
        return {};
    },
    getters: {
    },
    actions: {
    }
});

export default useReviewStore;
